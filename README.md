# OSStatus

Usage:

```
Usage: python -m osstatus [OPTIONS] COMMAND [ARGS]...

  get error code information from the OSStatus website

Options:
  --help  Show this message and exit.

Commands:
  get-all    return all errors
  print-all  print all errors to stdout
```
